# Local / private files — do not push
CLAUDE.md
README.md

# Dependencies
node_modules/

# Build output
dist/
out/
build/
release/

# Logs
*.log
npm-debug.log*

# Environment & secrets
.env
.env.*

# License signing secret — NEVER push. Anyone with this can mint valid keys.
# (license.secret.example.json IS committed as a template.)
license.secret.json
# Standalone generator has the secret baked in — NEVER push. Build it locally
# with `npm run license:standalone`.
license-generator/standalone.html
# Local activation record, if one is ever produced in the project folder.
license.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
