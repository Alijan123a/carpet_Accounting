/**
 * Drizzle ORM schema.
 *
 * Phase 0: intentionally EMPTY — no business tables yet.
 * Tables (clients, carpets, material, transactions, expenses, ...) are added
 * in Phase 1 per the domain model in CLAUDE.md, with indexes on every column
 * used in WHERE / ORDER BY / JOIN.
 *
 * Add table definitions below, e.g.:
 *   export const clients = sqliteTable('clients', { ... })
 */

export {}
